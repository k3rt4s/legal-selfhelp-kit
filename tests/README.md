# Tests

Pytest suite covering the optional tracker validator against conforming and non-conforming fixtures.

Run from the repository root:

```text
python -m pytest tests -q
```

The kit itself needs no tests, because it is Markdown, TOML, and CSV. These cover the one script that has logic.
