# Schemas

Field definitions for the structured records the model produces, so output stays consistent across sessions and across models.

## Contents

- [tracker.toml](tracker.toml): Column names, types, and allowed values for the tracker CSV.
- [issue.toml](issue.toml): The issue record the model emits during classification.
